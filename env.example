# Database Configuration
# For local development (without Docker)
DATABASE_URL=postgresql://ledger_user:ledger_password@localhost:5432/ledger_db

# For Docker Compose (use this when running in Docker)
# DATABASE_URL=postgresql://ledger_user:ledger_password@postgres:5432/ledger_db

# Server Configuration
PORT=3000
NODE_ENV=development

# Optional: Logging
LOG_LEVEL=info

